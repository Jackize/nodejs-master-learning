# Bắt buộc — app không start nếu thiếu
MONGODB_URI=mongodb://127.0.0.1:27017/shopstream
# Tuỳ chọn (có default trong Joi schema)
PORT=3000
NODE_ENV=development
JWT_SECRET=dev-secret-at-least-16-chars
JWT_EXPIRES_IN=86400